export interface Order {
  firstAndLastName: string |null,
  street: string | null,
  postalCode: string | null,
  city: string | null,
  phoneNumber: string | null,
  username: string,
  basketName: string,
}
